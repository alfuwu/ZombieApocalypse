using Terraria.ModLoader;
using ZombieApocalypse.Common.Extensions;

namespace ZombieApocalypse.Common.Commands;

/*public class Unzombify : ModCommand {
    public override CommandType Type => CommandType.Chat;

    public override string Command => "unzombify";
    public override string Description => "Unzombifies the player (debug command)";

    public override void Action(CommandCaller caller, string input, string[] args) {
        caller.Player.SetZombie(false);
        caller.Player.GetModPlayer<ZombifiablePlayer>().ClientHandleZombification();
    }
}*/